# OpenAI API Key (erforderlich)
OPENAI_API_KEY=sk-your-api-key-here

# Supabase (optional - nur für Lizenzverwaltung)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key

# Admin Dashboard Configuration
ADMIN_PASSWORD=change-this-to-secure-password

# Optional: Streamlit Configuration
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=0.0.0.0
 SHOP_BESCHREIBUNGEN.md, KOSTENOPTIMIERUNG.md, "Produktfoto Hausaufgabenhelfer_Tentary.png", assets, supabase, tools, .streamlit -DestinationPath "Hausaufgabenhelfer_Pro_Verkaufspaket.zip" -Force# Im Projektordner ausführen:
Compress-Archive -Path app.py, admin.py, requirements.txt, .env.example, Dockerfile, docker-compose.yml, Procfile, startup.sh, README.md, DOKUMENTATION.md, SCHNELLSTART.md, MARKETING.md, LIZENZ.md, VERKAUFSPAKET.md, SHOP_BESCHREIBUNGEN.md, KOSTENOPTIMIERUNG.md, "Produktfoto Hausaufgabenhelfer_Tentary.png", assets, supabase, tools, .streamlit -DestinationPath "Hausaufgabenhelfer_Pro_Verkaufspaket.zip" -Force# Im Projektordner ausführen:
Compress-Archive -Path app.py, admin.py, requirements.txt, .env.example, Dockerfile, docker-compose.yml, Procfile, startup.sh, README.md, DOKUMENTATION.md, SCHNELLSTART.md, MARKETING.md, LIZENZ.md, VERKAUFSPAKET.md, SHOP_BESCHREIBUNGEN.md, KOSTENOPTIMIERUNG.md, "Produktfoto Hausaufgabenhelfer_Tentary.png", assets, supabase, tools, .streamlit -DestinationPath "Hausaufgabenhelfer_Pro_Verkaufspaket.zip" -Force# Im Projektordner ausführen:
Compress-Archive -Path app.py, admin.py, requirements.txt, .env.example, Dockerfile, docker-compose.yml, Procfile, startup.sh, README.md, DOKUMENTATION.md, SCHNELLSTART.md, MARKETING.md, LIZENZ.md, VERKAUFSPAKET.md, SHOP_BESCHREIBUNGEN.md, KOSTENOPTIMIERUNG.md, "Produktfoto Hausaufgabenhelfer_Tentary.png", assets, supabase, tools, .streamlit -DestinationPath "Hausaufgabenhelfer_Pro_Verkaufspaket.zip" -Force# OpenAI API Key (erforderlich)
OPENAI_API_KEY=sk-your-api-key-here

# Supabase (optional - nur für Lizenzverwaltung)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key

# Admin Passwort
ADMIN_PASSWORD=your-secure-password
